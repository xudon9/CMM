/**
 * 测试 一般变量赋值
 * 2010-11-13
 * 邱忠磊
 * 
 * 测试 一般变量赋值 主要是：
 * 1.声明时赋值
 * 2.算术运算赋值
 * 3.read赋值
 * 4.变量传递，即把一个变量赋值给另外一个变量
 * 这里涉及到类型转换问题
 */

///////////////声明时赋值
int aa = 23,ab = (-4);
double b ,c =2.55;


/////////////算术运算赋值
int a = 15 - 3 * ( 150 / 3 / 10);
if(a == 0)
{
	println(1);
}
else
	println(0);

/////////////read赋值
double r;
r = todouble(read());
println(r);

//变量传递
double r2 = 5;
int a2 = 3;
r2 = a2;
if(r2 == 3)
{
	println(1);
}
else
	println(0);

r2 = a2 ;
println(r2);
/*output:
1
233			#这是输入的数字
233.0
1
3.0
*///:~
